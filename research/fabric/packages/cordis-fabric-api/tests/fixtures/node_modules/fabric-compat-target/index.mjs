/**
 * Plain target module for the compat adapter: a function with no cooperative
 * extension point (no event, no registry), reachable only through its
 * exported function. The compat adapter observes it through a low-level
 * Fabric patch.
 */

export function greet(name) {
  return `hello ${name}`
}
