// Fixture "official package" — target of tier-3 fabric transforms in e2e tests.
export function helper(text) {
  return '[util] ' + text
}

export function compute(n) {
  return n * 2
}

export function greet(name) {
  return 'hello, ' + name
}
