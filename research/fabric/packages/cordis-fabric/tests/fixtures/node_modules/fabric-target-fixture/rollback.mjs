// Top-level re-evaluation guard for the retransformEsm rollback case: the
// fixture throws when the env flag is set, so a re-import fails and the
// evicted loadCache entry must be restored.
if (process.env.DSH_FABRIC_ROLLBACK_BOOM === '1') {
  throw new Error('rollback fixture boom')
}

export const value = 1
