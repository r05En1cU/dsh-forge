export class Alpha {
  close(value) {
    return `alpha:${value}`
  }
}

export class Beta {
  close(value) {
    return `beta:${value}`
  }
}

export class Derived extends Alpha {
  constructor(value) {
    super(value)
    this.tag = 'derived'
  }
}
